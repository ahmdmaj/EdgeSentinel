
services:
  mosquitto:
    image: eclipse-mosquitto:2.0.18
    container_name: edgesentinel-mosquitto
    ports:
      - "${MQTT_PORT:-1883}:1883"
    volumes:
      - ./infrastructure/mosquitto/config:/mosquitto/config
    networks:
      - edgesentinel-network
    healthcheck:
      test: ["CMD-SHELL", "nc -z 127.0.0.1 1883 || exit 1"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 5s
    restart: unless-stopped

  mysql:
    image: mysql:8.0.36
    container_name: edgesentinel-mysql
    ports:
      - "${MYSQL_PORT:-3306}:3306"
    volumes:
      - mysql_data:/var/lib/mysql
    environment:
      MYSQL_ROOT_PASSWORD: ${MYSQL_PASSWORD}
      MYSQL_USER: ${MYSQL_USER}
      MYSQL_PASSWORD: ${MYSQL_PASSWORD}
      MYSQL_DATABASE: ${MYSQL_DATABASE}
    networks:
      - edgesentinel-network
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "localhost"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 10s
    restart: unless-stopped

  edge-service:
    build:
      context: ./services/edge
    container_name: edgesentinel-edge
    ports:
      - "${EDGE_PORT:-8000}:8000"
    environment:
      - MQTT_HOST=mosquitto
      - MQTT_PORT=1883
      - API_BASE_URL=http://cloud-api:3000
      - API_EMAIL=${API_EMAIL}
      - API_PASSWORD=${API_PASSWORD}
      - EDGE_ADMIN_TOKEN=${EDGE_ADMIN_TOKEN}
    volumes:
      - ./services/edge:/app
      - edge_outbox:/app/data
    networks:
      - edgesentinel-network
    depends_on:
      cloud-api:
        condition: service_healthy
      mosquitto:
        condition: service_healthy
    restart: unless-stopped

  cloud-api:
    build:
      context: ./apps/api
    container_name: edgesentinel-api
    ports:
      - "${API_PORT:-3000}:3000"
    environment:
      - DATABASE_URL=${DATABASE_URL}
      - JWT_SECRET=${JWT_SECRET}
      - SEED_ADMIN_EMAIL=${SEED_ADMIN_EMAIL}
      - SEED_ADMIN_PASSWORD=${SEED_ADMIN_PASSWORD}
    volumes:
      - ./apps/api/prisma:/app/prisma
      - ./apps/api/src:/app/src
    networks:
      - edgesentinel-network
    depends_on:
      mysql:
        condition: service_healthy
    healthcheck:
      test: ["CMD-SHELL", "curl -f http://127.0.0.1:3000/health || exit 1"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 10s
    restart: unless-stopped

  web:
    build:
      context: ./apps/web
    container_name: edgesentinel-web
    ports:
      - "${WEB_PORT:-3001}:3000"
    networks:
      - edgesentinel-network
    depends_on:
      cloud-api:
        condition: service_healthy
    restart: unless-stopped

  simulator:
    build:
      context: ./services/simulator
    container_name: edgesentinel-simulator
    environment:
      - MQTT_BROKER_URL=${MQTT_BROKER_URL}
    networks:
      - edgesentinel-network
    depends_on:
      mosquitto:
        condition: service_healthy
    restart: unless-stopped

  prometheus:
    image: prom/prometheus:v2.51.1
    container_name: edgesentinel-prometheus
    ports:
      - "${PROMETHEUS_PORT:-9090}:9090"
    volumes:
      - ./infrastructure/prometheus/prometheus.yml:/etc/prometheus/prometheus.yml
      - prometheus_data:/prometheus
    networks:
      - edgesentinel-network
    restart: unless-stopped

  grafana:
    image: grafana/grafana:10.4.2
    container_name: edgesentinel-grafana
    ports:
      - "${GRAFANA_PORT:-3002}:3000"
    volumes:
      - ./infrastructure/grafana/provisioning/datasources:/etc/grafana/provisioning/datasources
      - ./infrastructure/grafana/provisioning/dashboards:/etc/grafana/provisioning/dashboards
    networks:
      - edgesentinel-network
    depends_on:
      - prometheus
    restart: unless-stopped

networks:
  edgesentinel-network:
    name: edgesentinel-network
    driver: bridge

volumes:
  mysql_data:
  edge_outbox:
  prometheus_data:

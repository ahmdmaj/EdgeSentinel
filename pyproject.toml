[tool.pyrefly]
# Point Pyrefly to the local virtual environment for module resolution
python_interpreter = ".venv/Scripts/python.exe"

# Exclude Docker build artifacts and node modules from analysis
exclude = [
    ".venv",
    "node_modules",
    "**/dist/**",
    "**/__pycache__/**"
]
